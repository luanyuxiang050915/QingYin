# 《安卓视频去水印下载 App》项目计划书

> 版本：v0.1（立项评估稿）
> 日期：2026-08-11
> 目标：评估"用户粘贴链接 → 解析 → 下载无水印视频 → 保存到手机"的安卓 App 是否可行，并给出实施方案。

---

## 一、项目概述

**一句话目标**：做一个安卓 App（APK），用户粘贴短视频分享链接，App 自动解析出对应视频的**无水印版本**并下载保存到手机相册/文件。

**用户使用流程**：

1. 用户在某短视频 App（如抖音、快手、小红书）里点"分享 → 复制链接"；
2. 打开本 App，粘贴链接（支持自动读取剪贴板）；
3. 点击"解析"，App 展示视频信息（标题、封面、作者、时长、分辨率）；
4. 点击"下载"，带进度条下载无水印视频；
5. 下载完成后保存到系统相册，可随时查看/分享。

**交付物**：可直接安装的签名 APK（MVP 版本），以及后续维护各平台解析逻辑的源码工程。

---

## 二、可行性结论

### 2.1 结论

**技术上完全可行，市面上已有大量同类产品在运行**（如抖音/快手去水印下载器等，GitHub 上也有多个开源实现，例如 media-parser、ClipVault 等，可作为技术参考）。

但有一个关键前提必须先对齐，它决定了整个项目的工作量：

### 2.2 两种"去水印"，工作量天差地别

| 方式 | 原理 | 效果 | 工作量 |
| --- | --- | --- | --- |
| **A. 获取无水印源（推荐）** | 短视频平台本身就给同一个视频提供了两个播放地址：带水印版和不带水印版。解析器要做的只是从页面/接口中拿到**无水印直链**，然后直接下载，全程不碰视频画面。 | 原画质、秒级完成、手机端零压力 | 低，MVP 首选 |
| **B. 真·画面擦除（不推荐）** | 水印已经"烧"进视频画面里的情况（如他人二次剪辑、电视录制），需要用 AI 修复/重绘或裁掉水印区域再重编码。 | 依赖算法，可能模糊或有瑕疵 | 极高，需要 GPU/AI 模型，手机端几乎不现实，不建议作为本项目的方向 |

> **本项目默认采用方式 A**：针对抖音、快手、小红书、B 站、微博这类短视频平台，下载其无水印源。
> 如果你的真实需求是方式 B（任意视频擦除水印），那是一个完全不同量级的项目，需要另行评估（涉及 AI 视频修复、云端算力、成本等），计划书会单独说明。

### 2.3 核心原理示例（以抖音为例）

抖音的视频播放地址分两种，只差一个字段：

- 带水印：`https://aweme.snssdk.com/aweme/v1/playwm/?video_id=...`
- 无水印：`https://aweme.snssdk.com/aweme/v1/play/?video_id=...`（把 playwm 替换成 play）

解析流程本质上是：

```
分享文本 → 提取链接 → 跟随短链跳转拿到真实页面
    → 从页面 HTML / 内嵌接口中提取 video_id / 播放地址
    → 改写参数或调用接口拿到无水印直链 → 下载
```

> 该原理已通过公开资料验证，且截至 2025—2026 年仍适用于抖音等平台。但**平台会不定期改版、加风控**，这是本项目最大的维护性风险（详见第五章）。

### 2.4 各主流平台可行性评级

| 平台 | 难度 | 说明 |
| --- | --- | --- |
| 抖音 / 抖音极速版 | ★☆ 较易 | 原理公开（playwm → play），接口稳定度中等，解析资料最多 |
| TikTok | ★★ 中等 | 原理与抖音类似，但直链域名和风控不同，国内网络访问有额外问题 |
| 快手 | ★★ 中等 | 需要从页面接口提取，无公开的一键替换规律，但开源实现较多 |
| 小红书 | ★★★ 较难 | 接口签名/风控较严，笔记、图集、视频混合，解析成本高 |
| B 站 | ★ 易 | 官方提供公开的 view 接口，本就走"无水印"链路 |
| 微博 | ★★ 中等 | 接口较开放，但直链有时限、防盗链 |

**建议**：MVP 先只做抖音 + B 站（最容易、最稳定），跑通后再按需扩展。

---

## 三、目标平台与功能范围

### 3.1 MVP（第一版，目标 4~6 周）

- 支持平台：**抖音 + B 站**（验证全链路后再扩）；
- 粘贴/读取剪贴板链接，自动识别平台；
- 解析出视频标题、封面、作者、时长、清晰度，并展示；
- 下载无水印视频，显示实时进度，支持取消；
- 保存到系统相册（Android 10+ 走 MediaStore，无需存储权限）；
- 本地下载历史记录（可重复下载）；
- 基础界面：简洁单页式，首页 = 输入框 + 解析结果 + 下载按钮。

### 3.2 V2（第二版）

- 扩展平台：快手、小红书、微博、TikTok；
- 图集/多图下载、无水印封面与文案一键复制；
- 批量解析、下载队列、断点续传；
- 深色模式、国际化、分享入口；
- 可选：服务端代理解析（提高稳定性，见 4.5）。

### 3.3 明确不做（本期）

- 不做"真·AI 擦除水印"（方式 B）；
- 不做桌面端/网页端（先专注安卓 APK）；
- 不做付费会员/账号体系（先做工具型 App）。

---

## 四、技术方案

### 4.1 总体架构

```
┌─────────────────────────────────────────┐
│  UI 层：Jetpack Compose（或 XML）       │
│  输入框 / 结果卡片 / 下载进度 / 历史列表 │
└───────────────┬─────────────────────────┘
                ▼
┌─────────────────────────────────────────┐
│  业务层（MVVM + Repository）            │
│  LinkParser(识别平台) → PlatformParser   │
│  （每个平台一个解析器，工厂模式管理）    │
└───────────────┬─────────────────────────┘
                ▼
┌─────────────────────────────────────────┐
│  数据层                                  │
│  OkHttp 网络 / Room 历史记录 /           │
│  MediaStore 文件保存 / DownloadManager   │
└─────────────────────────────────────────┘
```

关键设计点：**解析器按平台隔离、可插拔**。平台改版时只改对应平台的解析器，不影响整体 App。

### 4.2 解析流程（详细）

1. **提取链接**：从分享文本中用正则提取 URL（分享文本常混有"复制此链接，打开抖音…"等文案）；
2. **跟随跳转**：短链（如 v.douyin.com/xxx）用 HTTP 302/301 拿到真实页面 URL；
3. **抓取页面**：请求真实页面 HTML，或直接命中其内嵌的 JSON 数据（如 _ROUTER_DATA、window.__INITIAL_STATE__ 等）；
4. **提取关键信息**：video_id、播放地址、标题、封面、作者；
5. **获取无水印直链**：改参数（抖音 playwm → play）或调用接口；
6. **下载**：OkHttp / DownloadManager 下载，带进度回调；
7. **保存**：写入 MediaStore（相册/电影目录），并插入下载历史。

### 4.3 技术栈与依赖

| 用途 | 选型 |
| --- | --- |
| 语言/UI | Kotlin + Jetpack Compose |
| 网络 | OkHttp + kotlinx.serialization（解析 JSON） |
| 页面解析 | jsoup（解析 HTML，兜底用） |
| 历史记录 | Room |
| 图片加载 | Coil（封面图） |
| 文件保存 | MediaStore + DownloadManager |
| 最低版本 | Android 8.0（API 26）+，覆盖绝大多数存量设备 |

### 4.4 权限与存储（重要）

- **Android 10（API 29）及以上**：用 MediaStore 保存到"电影/相册"，**不需要存储权限**，体验最好；
- **Android 9 及以下**：需要申请 WRITE_EXTERNAL_STORAGE 权限；
- **Android 13+**：下载通知需要申请 POST_NOTIFICATIONS 通知权限；
- **网络**：很多 CDN 直链是 http://，需要在 networkSecurityConfig 里允许明文流量（仅对下载域名白名单）；
- 全程无后台常驻、无敏感权限，容易被用户接受。

### 4.5 可选：服务端辅助解析

纯手机端解析能跑通 MVP，但有两个痛点：

- 平台风控可能对"同 IP 频繁解析"限流，手机 IP 容易被封；
- 部分平台需要签名参数（如 X-Bogus），逆向维护成本高。

**方案**：做一个小型后端（Python/FastAPI 或 Node.js），由服务器统一完成"链接解析 → 返回无水印直链"，App 只负责展示和下载。
**优点**：解析逻辑只维护一份、可换 IP 池、支持 Web 端/小程序复用。
**代价**：需要一台服务器、域名备案（国内）、流量成本。

> 建议：MVP 阶段纯客户端，跑通后再评估是否加服务端。

---

## 五、核心难点与应对

| 风险/难点 | 影响 | 应对 |
| --- | --- | --- |
| 平台接口改版、加风控 | 解析失效（最高风险） | 解析器可插拔；预留远端"规则配置"，可热更新解析规则而不发版；持续跟进开源社区 |
| 短链跳转/防盗链 | 拿不到真实地址 | 模拟移动端 UA、带 Referer、Cookie 保持 |
| 大视频下载失败 | 用户体验差 | 断点续传、失败重试、清晰度选择（省流量） |
| 安卓版本碎片化 | 兼容问题 | 最低 API 26；10+ 用 MediaStore，旧版用兼容方案 |
| 应用商店审核 | 可能被拒/下架 | 见第八章合规策略 |
| 版权纠纷 | 法律风险 | 工具仅供个人学习/素材管理，App 内加免责声明与举报入口 |

---

## 六、里程碑与排期

| 阶段 | 内容 | 预计工期 | 交付物 |
| --- | --- | --- | --- |
| **阶段 0：链路验证（POC）** | 用 Python/Postman 手工验证抖音、B 站"链接 → 无水印直链"全流程；确认各平台当前可用性 | 3~5 天 | 验证报告 + 可直接调用的解析脚本原型 |
| **阶段 1：MVP 单平台** | 安卓工程搭建；抖音解析器；输入/解析/下载/保存相册/历史记录；签名打包 | 2~3 周 | 可安装 APK（支持抖音） |
| **阶段 2：多平台扩展** | 接入 B 站、快手；解析器工厂完善；封面/标题展示优化；下载队列 | 2 周 | 多平台 APK |
| **阶段 3：打磨与发布** | 深色模式、通知权限适配、隐私政策、合规文案、真机全量测试、正式签名 | 1~2 周 | 正式版 APK + 上架材料 |

**合计：约 6~8 周出一个可用 MVP。**

---

## 七、资源需求

- **开发环境**：一台 Windows/Mac 电脑 + Android Studio（免费）；
- **测试设备**：至少 1 台安卓真机（建议 Android 12+ 和 Android 8~9 各一台，覆盖新旧系统）；
- **账号**：可选——GitHub（源码托管）、Google Play / 国内应用商店开发者账号（上架用）；
- **服务器**：MVP 不需要；若上服务端解析，需 1 台轻量云服务器（约 ¥50~100/月）；
- **人员**：1 名熟悉 Android 开发（Kotlin）的开发者即可完成 MVP；本人可全程协助编码。

---

## 八、法律与合规风险（务必认真看）

1. **平台条款**：抖音、快手等平台用户协议普遍禁止未经授权抓取、下载内容，去水印属于灰色地带；
2. **版权**：下载他人视频仅供**个人学习、素材管理**，用于商用或二次传播需原作者授权；
3. **上架风险**：
   - Google Play：以"视频下载器"名义上架有下架风险，很多同类 App 只能走官网/第三方分发；
   - 国内应用商店：审核更严格，多数"去水印"类 App 难以上架；
4. **合规建议**：
   - App 内显著位置放免责声明；
   - 不内置任何平台 Logo 擦除、绕过会员付费的功能；
   - 提供"内容侵权举报/删除"入口；
   - 最稳妥的合规定位是**个人学习、素材收集工具**，通过官网/群组分发 APK，而不是主打商店上架。

---

## 九、验收标准（MVP）

1. 粘贴抖音分享链接 → 5 秒内出结果（标题/封面/作者/清晰度）；
2. 下载无水印视频 → 视频里**无抖音水印**，画质与平台原画一致；
3. 下载完成后自动出现在系统相册，可播放、可分享；
4. 断网/链接失效时有明确错误提示；
5. 卸载重装后历史记录清空（或可迁移），无崩溃、无 ANR；
6. APK 正常签名安装，Android 8~15 真机测试通过。

---

## 十、下一步建议

如果方向 OK，建议按以下顺序推进：

1. **确认范围**：目标平台以哪些为主？（建议首期：抖音 + B 站）
2. **做 POC**：用 Python 快速验证当前平台解析链路（3~5 天，成本极低）；
3. **搭建安卓工程**：开始 MVP 开发；
4. **边做边评估**：是否引入服务端解析，取决于 POC 阶段平台风控的强度。

> 补充：如果你的真实需求是"**任意视频画面里的水印被 AI 擦除**"（方式 B），请告诉我，我会单独出一份 AI 视频修复方向的评估（涉及模型选型、云端算力与成本，量级与本文完全不同）。
